{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "run voxel_tracer",
            "program": "${workspaceRoot}/target/debug/voxel_tracer",
            "args": [],
            "cwd": "${workspaceRoot}",
            "sourceMap": {},
            "sourceLanguages": [
                "rust"
            ],
            "env": {
                "RUST_BACKTRACE": "short",
                "RUST_LOG": "info",
                "CARGO_MANIFEST_DIR": "${workspaceRoot}",
            }
        }
    ]
}